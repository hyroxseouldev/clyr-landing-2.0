# 코드팩토리 웹사이트 분석

분석 대상: `https://www.codefactory.ai/`  
분석 일자: 2026-09-07  
시각 기준: 사용자 제공 2560×6464 전체 화면 캡처와 실제 사이트의 데스크톱·모바일 렌더링

## 1. 기술 스택

- 프레임워크: Next.js 기반 사이트. `/_next/static/` 청크와 Next Image 최적화 URL을 사용한다.
- UI 구성: React 컴포넌트 기반이며 Tailwind CSS 유틸리티 클래스가 렌더된 DOM에서 확인된다.
- 스타일링: Tailwind CSS 4 계열의 CSS 변수·유틸리티 패턴을 사용한다.
- 이미지: Next Image를 통해 WebP로 전달한다. 도서 표지 3장과 YouTube 썸네일 3장이 주요 래스터 자산이다.
- 분석 시 확인된 외부 서비스: PostHog 계열 분석 스크립트가 로드된다.
- 본 로컬 복제본: React 19, Vite 6, Tailwind CSS 4, daisyUI 5, Lucide React, React Icons로 구성했다.

## 2. 폰트

- 본문·한글 디스플레이: `Pretendard`, fallback `system-ui, sans-serif`
- 영문 내비게이션·섹션 라벨: `JetBrains Mono`, fallback `monospace`
- 원본에서 로드되는 두 WOFF2 폰트를 로컬 자산으로 보존했다.
- H1 데스크톱: 72px / 700 / 1.14 / -0.02em
- H1 모바일: 38px / 700 / 1.14 / -0.02em
- H2 데스크톱: 40px / 600 / 1.2 / -0.02em
- H2 모바일: 26px / 600 / 1.2 / -0.02em
- 내비게이션: 11px / 500 / 1 / 0.16em
- 섹션 라벨: 10–11px / 500 / 1 / 0.16–0.2em

## 3. 컬러와 시각 토큰

| 용도 | 값 |
|---|---|
| 페이지 배경 | `#08090c` |
| 패널 배경 | `#0c0e12` |
| 기본 전경 | `#f2f3f5` |
| 약한 텍스트 | `#888e9c` 또는 전경색 45–60% |
| 주요 액센트 | `#4a6fd4` |
| 청록 액센트 | `#22d3ee` |
| 링크 포인트 | `#9db6f2` |
| 카드 테두리 | `rgba(255,255,255,.08)` |
| 입력 테두리 | `rgba(255,255,255,.11)` |

- 배경에는 88×88px의 1px 그리드가 깔린다.
- 그리드는 상단 중심의 큰 방사형 마스크로 가장자리에서 사라진다.
- 히어로 좌상단과 우하단에 블루 글로우 오브가 배치된다.
- 문의 섹션 하단에는 청록–블루 방사형 글로우가 있다.
- 기본 카드 반경은 14–18px, 버튼은 999px 캡슐형이다.

## 4. 레이아웃

- 전체 콘텐츠 셸: 최대 1224px, 좌우 내부 여백 32px
- 실제 읽기 영역: 1160px
- 고정 헤더: 높이 65px, 배경 `rgba(8,9,12,.72)`, 14px backdrop blur
- 데스크톱 기준 주요 섹션 높이:
  - Hero: 1318px
  - Collaborations: 422px
  - Numbers: 780px
  - Services: 911px
  - Books: 약 977px
  - YouTube: 약 590px
  - Contact: 975px
  - Footer: 490px
- 각 본문 섹션은 대체로 상단 150px, 하단 40px 여백을 사용한다.
- 서비스 섹션은 340px 요약 열과 나머지 목록 열의 2열 구조다.
- 도서·YouTube 영역은 데스크톱 3열, 모바일 1열이다.

## 5. 반응형 규칙

- 주요 모바일 기준점은 640px 미만이다.
- 모바일 콘텐츠 좌우 여백은 32px이며 390px 화면에서 실제 콘텐츠 폭은 326px이다.
- 데스크톱 내비게이션은 숨기고 CONTACT 버튼과 원형 메뉴 버튼만 노출한다.
- 모바일 Hero는 844px 높이, H1은 세 줄로 줄바꿈된다.
- 서비스는 단일 열로 바뀌며 항목의 우측 화살표가 숨겨진다.
- 도서와 영상 카드는 모두 세로 스택으로 전환된다.
- 원본 모바일에서 Contact의 DIRECT 카드 영역은 보이지 않으므로 복제본에서도 숨겼다.
- 모바일 전체 화면의 섹션별 높이와 큰 여백을 유지해 원본의 느슨한 편집 리듬을 보존했다.

## 6. 구성 요소와 인터랙션

- 헤더: 앵커 기반 부드러운 스크롤, 모바일 토글 메뉴
- Hero: 문의 섹션 이동, YouTube 외부 링크
- 협업 브랜드: 무한 수평 마키, hover/focus 시 일시 정지
- 수치: 라벨·대형 수치·블루–청록 분할 라인
- 서비스: 4개 행, hover 배경 변화, 문의 섹션 이동
- 도서: 3개 외부 링크 카드, hover 시 상승·테두리 강조
- YouTube: 3개 썸네일 카드, 중앙 재생 아이콘, 외부 링크
- 문의 유형: 5개 토글형 버튼 중 단일 선택
- 이메일: 클릭 시 `jc@codefactory.ai` 클립보드 복사와 짧은 완료 상태
- 문의 폼: 로컬 데모 상태만 변경하며 외부로 데이터를 전송하지 않는다.
- 섹션 제목: IntersectionObserver 기반 위로 떠오르는 reveal 효과
- 접근성: focus-visible 청록색 외곽선, 버튼 상태의 `aria-pressed`, 메뉴의 `aria-expanded`

## 7. 로컬로 보존한 원본 자산

- 브랜드 로고 16종 SVG
- 코드팩토리 심볼 SVG
- 도서 표지 3종 WebP
- YouTube 썸네일 3종 WebP
- Pretendard WOFF2
- JetBrains Mono WOFF2

최종 구현은 원본 사이트 자산을 hotlink하지 않고 `/public/assets/` 아래 로컬 파일만 사용한다.

## 8. 복제 시 주의사항

- 원본 사이트의 저작권·상표·이용약관을 준수하고, 소유권 또는 재현 권한이 있는 범위에서만 사용해야 한다.
- 외부 도서·채널 링크는 원본 목적지로 유지했지만 문의 전송은 의도적으로 비활성화한 프론트엔드 데모다.
- 원본 분석/마케팅 스크립트는 복제하지 않았다.
